{
  "type": "module",
  "private": true,
  "scripts": {
    "backend:dev": "cd api && go run .",
    "frontend:dev": "cd frontend && npm run dev",
    "build:backend": "cd api && go build -o server .",
    "build:frontend": "cd frontend && npm run build && cpy dist ../",
    "dev": "cross-env IMAGE_PATH=F:/photography ADMIN_NAME=admin ADMIN_PASSWORD=password concurrently \"npm run frontend:dev\" \"npm run backend:dev\"",
    "build": "cross-env CGO_ENABLED=0 npm run build:frontend && npm run build:backend",
    "cspell": "cspell \"**/*.{go,ts,js,vue}\""
  },
  "devDependencies": {
    "cpy-cli": "^5.0.0",
    "cross-env": "^7.0.3",
    "cspell": "^8.15.4",
    "env-cmd": "^10.1.0"
  },
  "dependencies": {
    "concurrently": "^9.0.1"
  }
}
