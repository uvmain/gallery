services:
  gallery:
    image: gallery:latest
    container_name: gallery
    environment:
      THUMBNAIL_MAX_PIXELS: 500
      OPTIMISED_MAX_PIXELS: 1280
    volumes:
      - /mnt/main/docker/gallery:/data
      - /mnt/barrra5/photography:/photos
    ports:
      - "3001:8080"